# 仓库顶层铁律

本仓库（myscoop）的全局守则。**带"铁"字号的规矩，违反一条都算事故。**

## 壹号铁律：gzh/ 属本地仓库一部分，但绝不提交推送

1. `gzh/` 是**本仓库工作区的一部分**（位于仓库内、属于本地仓库资产），但它承载个人公众号内容（文章/图文/封面），**隐私性质，永不公开**。
2. 因此：`gzh/` 的一切文件**绝不进入任何 commit，绝不 push**——包括 `git add gzh`、`git add -f gzh/...`、`git commit gzh ...` 等一切形式。
3. `.gitignore` 中的 `gzh/` 条目**永远不许删除**（它是第四条保险）。
4. 每个 commit 前后必须 `git status --short` 确认**没有任何 gzh/ 路径出现**；CI 机器人（auto-update 等）的输出也不得包含 gzh 内容。
5. 若发现 gzh 内容误入提交历史：立即 `git rm --cached` 并重写受影响的历史，向用户汇报事故经过。
6. 本地备份与同步 gzh/ 数据**只允许通过私有手段**（本地拷贝/私有云端），不允许通过公开仓库。

## 贰号铁律：工作区文件归属

1. `bucket/`、`gzh/`、`staging/` 分工：bucket 是正式清单（公开提交）；gzh 是本地私有文章资产（绝不提交）；staging 是草稿区（gitignore，不提交）。
2. 只有存储库公开资产（bucket、脚本、文档、工具）才能进 commit。

## 叁号铁律：提交守则

1. 提交信息用中文，聚焦变更要点；常规双提交：功能提交 + `progress.md 更新提交历史`（由 tools/rebuild_progress.py 重建）。
2. push 前必须 `git pull --rebase --autostash`；与 CI bot 冲突时按 progress.md 重建流程解决。
3. 任何一次提交都不允许夹带 gzh/、staging/、.playwright-cli/ 内容（子目录铁律见 gzh/铁律.md）。